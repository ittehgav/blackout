[gd_scene load_steps=9 format=3 uid="uid://csfb4spydffb5"]

[ext_resource type="Script" uid="uid://baihpjue8prfg" path="res://scenes/test/combat/units/fighter_test.gd" id="1_rgl56"]
[ext_resource type="PackedScene" uid="uid://lls0f7qofwnf" path="res://scenes/leader/roster/fighter/fighter_unit.tscn" id="2_eouuu"]
[ext_resource type="PackedScene" uid="uid://d1eptkluku252" path="res://scenes/index/fighters/special/dummy.tscn" id="3_6mpsc"]
[ext_resource type="Script" uid="uid://df75lw20fhu53" path="res://scenes/test/combat/units/analytic.gd" id="4_eouuu"]

[sub_resource type="Shader" id="Shader_ic6ef"]
code = "shader_type canvas_item;

uniform vec4 target_color :source_color=vec4(0.0,0.0,0.0,1.0);
uniform float grad:hint_range(0.0, 1.0);


uniform vec4 color : source_color=vec4(1.0,0.0,1.0,1.0);
uniform float width : hint_range(0, 10) = 1.0;
uniform int pattern : hint_range(0, 2) = 0; // diamond, circle, square
uniform bool inside = false;
uniform bool add_margins = true; // only useful when inside is false
uniform vec2 number_of_images = vec2(1.0); // number of horizontal and vertical images in the sprite sheet

varying flat vec4 modulate;

void vertex() {
	modulate = COLOR;

	if (add_margins) {
		if ((UV.x <= 0.0 || UV.x >= 1.0) && (UV.y <= 0.0 || UV.y >= 1.0)) {
			VERTEX += (UV * 2.0 - 1.0) * width;
		} else {
			VERTEX += sign(VERTEX) * width; // replace sign(VERTEX) by (sign(VERTEX) * 2.0 - 1.0) if your AnimatedSprite2D isn't Centered
		}
	}
}

bool hasContraryNeighbour(vec2 uv, vec2 texture_pixel_size, vec2 image_top_left, vec2 image_bottom_right, sampler2D texture) {
	for (float i = -ceil(width); i <= ceil(width); i++) {
		float x = abs(i) > width ? width * sign(i) : i;
		float offset;

		if (pattern == 0) {
			offset = width - abs(x);
		} else if (pattern == 1) {
			offset = floor(sqrt(pow(width + 0.5, 2) - x * x));
		} else if (pattern == 2) {
			offset = width;
		}

		for (float j = -ceil(offset); j <= ceil(offset); j++) {
			float y = abs(j) > offset ? offset * sign(j) : j;
			vec2 xy = uv + texture_pixel_size * vec2(x, y);

			if ((xy != clamp(xy, image_top_left, image_bottom_right) || texture(texture, xy).a <= 0.0) == inside) {
				return true;
			}
		}
	}

	return false;
}


void fragment(){

	// OUTLINE

	vec2 uv = UV;
	vec2 image_top_left = floor(uv * number_of_images) / number_of_images;
	vec2 image_bottom_right = image_top_left + vec2(1.0) / number_of_images;

	if (add_margins) {
		vec2 texture_pixel_size = vec2(1.0) / (vec2(1.0) / TEXTURE_PIXEL_SIZE + vec2(width * 2.0) * number_of_images);

		uv = (uv - texture_pixel_size * width - image_top_left) * TEXTURE_PIXEL_SIZE / texture_pixel_size + image_top_left;

		if (uv != clamp(uv, image_top_left, image_bottom_right)) {
			COLOR.a = 0.0;
		} else {
			COLOR = texture(TEXTURE, uv) * modulate;
		}
	} else {
		COLOR = texture(TEXTURE, uv) * modulate;
	}

	if ((COLOR.a > 0.0) == inside && hasContraryNeighbour(uv, TEXTURE_PIXEL_SIZE, image_top_left, image_bottom_right, TEXTURE)) {
		COLOR.rgb = inside ? mix(COLOR.rgb, color.rgb * modulate.rgb, color.a * modulate.a) : color.rgb * modulate.rgb;
		COLOR.a += (1.0 - COLOR.a) * color.a * modulate.a;
	}

	// BLINK ANIMATIONS
	if(grad != 0.0 && COLOR.a != 0.0){
		COLOR = mix(COLOR, target_color, grad);
	}
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_4qx7v"]
resource_local_to_scene = true
shader = SubResource("Shader_ic6ef")
shader_parameter/target_color = Color(0, 0, 0, 1)
shader_parameter/grad = 0.0
shader_parameter/color = Color(0.1, 0.1, 0.5, 1)
shader_parameter/width = 1.0
shader_parameter/pattern = 0
shader_parameter/inside = false
shader_parameter/add_margins = true
shader_parameter/number_of_images = Vector2(1, 1)

[sub_resource type="ShaderMaterial" id="ShaderMaterial_rgl56"]
resource_local_to_scene = true
shader = SubResource("Shader_ic6ef")
shader_parameter/target_color = Color(0, 0, 0, 1)
shader_parameter/grad = 0.0
shader_parameter/color = Color(0.1, 0.1, 0.5, 1)
shader_parameter/width = 1.0
shader_parameter/pattern = 0
shader_parameter/inside = false
shader_parameter/add_margins = true
shader_parameter/number_of_images = Vector2(1, 1)

[sub_resource type="ShaderMaterial" id="ShaderMaterial_eouuu"]
resource_local_to_scene = true
shader = SubResource("Shader_ic6ef")
shader_parameter/target_color = Color(0, 0, 0, 1)
shader_parameter/grad = 0.0
shader_parameter/color = Color(0.1, 0.1, 0.5, 1)
shader_parameter/width = 1.0
shader_parameter/pattern = 0
shader_parameter/inside = false
shader_parameter/add_margins = true
shader_parameter/number_of_images = Vector2(1, 1)

[node name="Fighter Test" type="Node2D" node_paths=PackedStringArray("unit", "dummies", "analytic")]
script = ExtResource("1_rgl56")
unit = NodePath("unit")
dummies = [NodePath("dummy"), NodePath("dummy2"), NodePath("dummy3")]
analytic = NodePath("analytic")

[node name="dummy" parent="." instance=ExtResource("2_eouuu")]
visible = true
position = Vector2(309, 58)

[node name="Dummy" parent="dummy" instance=ExtResource("3_6mpsc")]
material = SubResource("ShaderMaterial_4qx7v")

[node name="dummy2" parent="." instance=ExtResource("2_eouuu")]
visible = true
position = Vector2(194, 110)

[node name="Dummy" parent="dummy2" instance=ExtResource("3_6mpsc")]
material = SubResource("ShaderMaterial_rgl56")

[node name="dummy3" parent="." instance=ExtResource("2_eouuu")]
visible = true
position = Vector2(235, -62)

[node name="Dummy" parent="dummy3" instance=ExtResource("3_6mpsc")]
material = SubResource("ShaderMaterial_eouuu")

[node name="unit" parent="." instance=ExtResource("2_eouuu")]
visible = true
position = Vector2(-544, 52)

[node name="analytic" type="Control" parent="." node_paths=PackedStringArray("level_label", "dps_label")]
layout_mode = 3
anchors_preset = 0
offset_right = 40.0
offset_bottom = 40.0
script = ExtResource("4_eouuu")
level_label = NodePath("VBoxContainer/level")
dps_label = NodePath("VBoxContainer/dps")

[node name="VBoxContainer" type="VBoxContainer" parent="analytic"]
layout_mode = 0
offset_right = 40.0
offset_bottom = 40.0

[node name="level" type="Label" parent="analytic/VBoxContainer"]
layout_mode = 2
theme_override_font_sizes/font_size = 16
text = "Level:"

[node name="dps" type="Label" parent="analytic/VBoxContainer"]
layout_mode = 2
theme_override_font_sizes/font_size = 16
text = "DPS: "

[node name="stun_uptime" type="Label" parent="analytic/VBoxContainer"]
layout_mode = 2
theme_override_font_sizes/font_size = 16
text = "Stun Uptime"

[node name="refresh" type="Timer" parent="analytic/VBoxContainer"]
autostart = true

[connection signal="timeout" from="analytic/VBoxContainer/refresh" to="analytic" method="refresh"]
