extends Sprite2D

## big and slow weapon that fires a wrecking ball because why not
const type="ranged";
const projectile_lifespan = 1;
const projectile_speed = 500;

@export var projectile:RigidBody2D;
