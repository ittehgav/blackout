extends Weapon

const size_x = 4;
const size_y = 2

const rarity = 2;

func get_description()->String:
	return "Hold to charge an attack in the direction of the cursor, when released, slice through enemies in the trajectory.";
