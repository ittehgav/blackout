[gd_scene load_steps=8 format=3 uid="uid://bteusaeptopnl"]

[ext_resource type="Script" uid="uid://c1stg5kmtc68e" path="res://scenes/index/perks/scripts/financial_boost.gd" id="1_nwnbf"]
[ext_resource type="Texture2D" uid="uid://br4sa081ylalm" path="res://assets/visual/icons/resources/money.png" id="2_k171s"]
[ext_resource type="PackedScene" uid="uid://c0cfd3mr7dsam" path="res://scenes/ui/icons/dropdowns/resources_dropdown.tscn" id="3_secrd"]
[ext_resource type="AudioStream" uid="uid://c4bg8a6ld74uf" path="res://assets/sound/sfx/ui/inventory/resources/money_change.wav" id="3_yux3d"]
[ext_resource type="PackedScene" uid="uid://b3q2rj3eol6t" path="res://scenes/battle/fighter/player/body.tscn" id="4_yux3d"]

[sub_resource type="Shader" id="Shader_4frhf"]
code = "shader_type canvas_item;

uniform vec4 target_color :source_color=vec4(0.0,0.0,0.0,1.0);
uniform float grad:hint_range(0.0, 1.0);


uniform vec4 color : source_color=vec4(1.0,0.0,1.0,1.0);
uniform float width : hint_range(0, 10) = 1.0;
uniform int pattern : hint_range(0, 2) = 0; // diamond, circle, square
uniform bool inside = false;
uniform bool add_margins = true; // only useful when inside is false
uniform vec2 number_of_images = vec2(1.0); // number of horizontal and vertical images in the sprite sheet

varying flat vec4 modulate;

void vertex() {
	modulate = COLOR;

	if (add_margins) {
		if ((UV.x <= 0.0 || UV.x >= 1.0) && (UV.y <= 0.0 || UV.y >= 1.0)) {
			VERTEX += (UV * 2.0 - 1.0) * width;
		} else {
			VERTEX += sign(VERTEX) * width; // replace sign(VERTEX) by (sign(VERTEX) * 2.0 - 1.0) if your AnimatedSprite2D isn't Centered
		}
	}
}

bool hasContraryNeighbour(vec2 uv, vec2 texture_pixel_size, vec2 image_top_left, vec2 image_bottom_right, sampler2D texture) {
	for (float i = -ceil(width); i <= ceil(width); i++) {
		float x = abs(i) > width ? width * sign(i) : i;
		float offset;

		if (pattern == 0) {
			offset = width - abs(x);
		} else if (pattern == 1) {
			offset = floor(sqrt(pow(width + 0.5, 2) - x * x));
		} else if (pattern == 2) {
			offset = width;
		}

		for (float j = -ceil(offset); j <= ceil(offset); j++) {
			float y = abs(j) > offset ? offset * sign(j) : j;
			vec2 xy = uv + texture_pixel_size * vec2(x, y);

			if ((xy != clamp(xy, image_top_left, image_bottom_right) || texture(texture, xy).a <= 0.0) == inside) {
				return true;
			}
		}
	}

	return false;
}


void fragment(){

	// OUTLINE

	vec2 uv = UV;
	vec2 image_top_left = floor(uv * number_of_images) / number_of_images;
	vec2 image_bottom_right = image_top_left + vec2(1.0) / number_of_images;

	if (add_margins) {
		vec2 texture_pixel_size = vec2(1.0) / (vec2(1.0) / TEXTURE_PIXEL_SIZE + vec2(width * 2.0) * number_of_images);

		uv = (uv - texture_pixel_size * width - image_top_left) * TEXTURE_PIXEL_SIZE / texture_pixel_size + image_top_left;

		if (uv != clamp(uv, image_top_left, image_bottom_right)) {
			COLOR.a = 0.0;
		} else {
			COLOR = texture(TEXTURE, uv) * modulate;
		}
	} else {
		COLOR = texture(TEXTURE, uv) * modulate;
	}

	if ((COLOR.a > 0.0) == inside && hasContraryNeighbour(uv, TEXTURE_PIXEL_SIZE, image_top_left, image_bottom_right, TEXTURE)) {
		COLOR.rgb = inside ? mix(COLOR.rgb, color.rgb * modulate.rgb, color.a * modulate.a) : color.rgb * modulate.rgb;
		COLOR.a += (1.0 - COLOR.a) * color.a * modulate.a;
	}

	// BLINK ANIMATIONS
	if(grad != 0.0 && COLOR.a != 0.0){
		COLOR = mix(COLOR, target_color, grad);
	}
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_hv8o2"]
resource_local_to_scene = true
shader = SubResource("Shader_4frhf")
shader_parameter/target_color = Color(0, 0, 0, 1)
shader_parameter/grad = 0.0
shader_parameter/color = Color(0.1, 0.1, 0.5, 1)
shader_parameter/width = 2.0
shader_parameter/pattern = 0
shader_parameter/inside = false
shader_parameter/add_margins = true
shader_parameter/number_of_images = Vector2(1, 1)

[node name="Financial Boost" type="Node" node_paths=PackedStringArray("money_label", "panel", "sfx")]
script = ExtResource("1_nwnbf")
money_label = NodePath("Panel/resources_dropdown/money/money")
icon = ExtResource("2_k171s")
panel = NodePath("Panel")
sfx = NodePath("sfx")
metadata/_custom_type_script = "uid://cvor0b75qlqh5"

[node name="sfx" type="AudioStreamPlayer" parent="."]
stream = ExtResource("3_yux3d")

[node name="Panel" type="Panel" parent="."]
visible = false
anchors_preset = 8
anchor_left = 0.5
anchor_top = 0.5
anchor_right = 0.5
anchor_bottom = 0.5
offset_left = -92.5
offset_top = -138.5
offset_right = 92.5
offset_bottom = 138.5
grow_horizontal = 2
grow_vertical = 2
mouse_filter = 2

[node name="resources_dropdown" parent="Panel" node_paths=PackedStringArray("resource_hboxes", "resource_icons") instance=ExtResource("3_secrd")]
layout_mode = 1
anchors_preset = 7
anchor_top = 1.0
anchor_bottom = 1.0
offset_left = -52.0
offset_top = -63.0
offset_right = 52.0
offset_bottom = -11.0
grow_vertical = 0
resource_hboxes = {
"chips": NodePath("chips"),
"food": NodePath("food"),
"fuel": NodePath("fuel"),
"juice": NodePath("juice"),
"money": NodePath("money"),
"scrap": NodePath("scrap")
}
resource_icons = {
"chips": NodePath("chips/chips_icon"),
"food": NodePath("food/food_icon"),
"fuel": NodePath("fuel/fuel_icon"),
"juice": NodePath("juice/juice_icon"),
"money": NodePath("money/money_icon"),
"scrap": NodePath("scrap/scrap_icon")
}

[node name="money" parent="Panel/resources_dropdown" index="0"]
mouse_filter = 2

[node name="money_icon" parent="Panel/resources_dropdown/money" index="0"]
mouse_filter = 2

[node name="HSeparator" parent="Panel/resources_dropdown" index="1"]
visible = false
mouse_filter = 2

[node name="food" parent="Panel/resources_dropdown" index="2"]
visible = false
mouse_filter = 2

[node name="food_icon" parent="Panel/resources_dropdown/food" index="0"]
mouse_filter = 2

[node name="fuel" parent="Panel/resources_dropdown" index="3"]
visible = false
mouse_filter = 2

[node name="fuel_icon" parent="Panel/resources_dropdown/fuel" index="0"]
mouse_filter = 2

[node name="juice" parent="Panel/resources_dropdown" index="4"]
visible = false
mouse_filter = 2

[node name="juice_icon" parent="Panel/resources_dropdown/juice" index="0"]
mouse_filter = 2

[node name="scrap" parent="Panel/resources_dropdown" index="5"]
visible = false
mouse_filter = 2

[node name="scrap_icon" parent="Panel/resources_dropdown/scrap" index="0"]
mouse_filter = 2

[node name="chips" parent="Panel/resources_dropdown" index="6"]
visible = false
mouse_filter = 2

[node name="chips_icon" parent="Panel/resources_dropdown/chips" index="0"]
mouse_filter = 2

[node name="body" parent="Panel" instance=ExtResource("4_yux3d")]
process_mode = 4
material = SubResource("ShaderMaterial_hv8o2")
position = Vector2(91, 101)
scale = Vector2(4, 4)

[editable path="Panel/resources_dropdown"]
